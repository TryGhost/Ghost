# Member Detail React migration — progress log

Working branch: `zimo/member-details-react-spike`
Plan: `~/.claude/plans/i-want-to-work-clever-lynx.md`
Cadence: red→green TDD, narrow vertical slices, quality checks + browser check + adversarial sub-agent review + Codex (highest effort) second opinion at the end of each slice, commit as I go.

Dev stack is live: `apps/admin` vite dev server on :5174 (base `/__admin-dev__`) proxied through `localhost:2368/ghost`, importing `@tryghost/posts` from source → my changes hot-reload in the browser.

---

## Phase 1 — Framework hooks ✅ (done before goal set)
Added 7 hooks + types to `apps/admin-x-framework/src/api/members.ts` (`useEditMember`, `useDeleteMember`, `getMemberSigninUrl`, `useMemberLogout`, `useEditMemberSubscription`, `useRemoveMemberEmailSuppression`, `useMemberActivityFeed`) with 11 unit tests. 447/447 package tests pass, lint clean, 0 new tsc errors. Not yet committed (committing in Phase 2 slices).

---

## Phase 2 — Read-only detail scaffold (in progress)

Slices:
- **2.1** Route flip + pipeline tracer: `./member-detail` export, minimal `member-detail.tsx` (title + back link + loading/not-found), remove `/members/:member_id` from EMBER_ROUTES + register React route. Seam under test (unit): `deriveMemberDetailBackPath(search)`.
- **2.2** Read-only sidebar (avatar, geolocation, created/last-seen, email open rate, attribution, Stripe customer link). Seams: `parseGeolocation`, display helpers.
- **2.3** Read-only subscriptions + newsletters display (no actions yet).

### Deviations from plan
- **D1 (corrected):** Initially flipped `/members/:member_id` to React in Phase 2, then **reverted** — flipping early breaks the whole `e2e/tests/admin/members-legacy` suite (impersonation/commenting/etc.), which is precisely why the plan sequences the flip last. Instead the WIP React screen is mounted on a **temporary preview route** `/members/preview/:member_id` (child of `membersRoute`, shares the `canManageMembers` guard). Ember keeps `/members/:member_id`; existing e2e stays green. Phase 8 renames the preview route to `:member_id`, removes the EMBER_ROUTES entry, and rewires nav.
- **D2:** Detail renders *within* admin chrome (sidebar visible), matching Ember + the members list, instead of the plan's `hideAdminSidebar` full-screen style. Reversible via the route handle.
- **Browser verification mechanism:** live-browser checks against the dev stack need an authenticated session; the auto-mode guardrail (correctly) blocks materializing the admin session secret to forge a cookie, and no Chrome extension is connected. So browser verification uses the **Playwright e2e harness in dev mode** (auto-detects the :5174 dev server, provisions its own isolated Ghost + owner, reflects my HMR'd source). This doubles as the "favor e2e" requirement.

### Tooling notes
- **Codex integration:** was unavailable during Phase 1–2 (corrupted `@openai/codex` install — missing vendored binary), so slices 2.1–2.3 used an adversarial general-purpose sub-agent as the second opinion. **User reinstalled Codex; now ready** (`codex-cli 0.142.5`, advanced runtime, ChatGPT auth active) — from Phase 3 onward the Codex second opinion runs alongside the adversarial sub-agent review. Review gate left disabled; Codex invoked explicitly per slice.

### Log
- **slice 6.1 — Remove complimentary subscription (Phase 6):** comp subscription rows now have their own actions menu with a single **"Remove complimentary subscription"** destructive item that opens a confirm `AlertDialog`. On confirm, calls `useEditMember({id, email, tiers: <survivors>})`. The trigger + dialog stay busy through the invalidated members refetch (`useIsFetching(['MembersResponseType'])`) so a spam-click can't fire the removal twice. Gift subs still show no menu (Ember parity).
  - **Codex + adversarial reviews: both landed and folded in.** Real bugs both flagged and fixed pre-commit:
    - **Multi-tier expiry_at data loss** (P0, both reviewers): the naive `[{id}]` payload for surviving tiers would silently `null` their `expiry_at` on the server pivot update (`member.js:305-315:updateTierExpiry`). Fixed by preserving `expiry_at` on every survivor, and pinned by a new e2e test that seeds a member with two comp tiers, removes one, and asserts the survivor's expiry_at rides along.
    - **Unreachable loading UI in the confirm dialog** (P1, adversarial): `AlertDialogAction asChild` auto-closes the dialog on click, so my `LoadingIndicator` inside the Remove button was dead code — Radix closed the dialog before `editMember.isLoading` flipped. Swapped `AlertDialogAction` for a plain `Button` inside the footer so dialog closure is owned by `onSuccess`, and the loading spinner actually renders now.
    - **Missing `email` in payload** (P1, adversarial): Ember sends `{id, email, tiers}` (`gh-member-settings-form.js:194-198`); mine sent `{id, tiers}`. Server tolerates both but aligned to Ember exactly for safety. E2e assertions updated.
  - Non-issues confirmed clean: `tiers` → `products` server mapping via input serializer, `member.tiers[i].id === sub.tier.id` (same identifier), server drops status → `free` automatically when the last product is removed, no double-fire race, focus management via Radix.
  - Quality: unit 87/87, eslint clean, my files 0 tsc errors, **e2e now 20 tests** including two new "removes a comp" + "preserves expiry_at" tests.
- **slice 5.4 — form + subscription-row UI to match Ember (post-Phase 5 refinement):** proper visual alignment with the Ember reference, based on side-by-side screenshot comparison (added a real Playwright screenshot spec + iterated against the user's Ember screenshots).
  - **Form**: Name and Email now sit side-by-side in a two-column grid, dropped the artificial `max-w-xl` so the form fills the card, note field gained the "(not visible to member)" hint, and the character counter now reads "Maximum: 500 characters. You've used N" like Ember.
  - **Sections**: newsletters is a self-contained section (external "NEWSLETTERS" heading + own Card); it hides the WHOLE section — heading and card — when there are no active newsletters, so the parent never has to know the newsletter count. Subscriptions section has the same "SUBSCRIPTIONS" external heading pattern, and the internal row lost its double border (only the Card border shows).
  - **Subscription row redesign**: a big price block on the left (`$5` big + `monthly` small in a rounded muted square, or "Comp"/"Gift" text for non-paid), tier + status badge in the middle, priceLabel + validity line (now "Free trial – Ends 2 Aug 2026", "Complimentary – Expires 15 Jan 2027", etc.), a "Details" toggle that expands to Created / Source / Page, and the actions "…" menu on the far right (paid only). `getSubscriptionPriceLabel` extended to return "Free trial" / the price nickname (Complimentary / Gift Subscription), covered by five new unit tests.
  - Framework `MemberSubscription` type widened with the previously-missing `attribution` block that Ember uses for Source / Page in the details.
  - Widened the e2e assertions to match the new element structure (price now split across three spans/divs) and pinned the exact "Free trial – Ends" copy.
  - **Visual verification tooling**: added `apps/posts/tmp/` (gitignored) + a Playwright screenshot spec (`e2e/tests/admin/members/member-detail-screenshots.test.ts`) that renders the paid-subscription screen against injected data and saves PNGs. Used it to iterate against Ember screenshots. **User has since asked to stop screenshot iteration**, so it's not used from here on — the spec stays for a future re-enable.
  - Quality: detail unit suite 87/87, eslint clean, my files 0 tsc errors, **e2e 18/18** on `tests/admin/members/member-detail.test.ts`.
- **slice 5.3 — UI polish to match Ember (post-Phase 5 refinement):** design pass folding in Peter's manual-testing feedback about the visual gap vs the Ember screen.
  - Header: swapped the ghost back arrow for a **Shade breadcrumb** — `Members > <member name>` (or `Members > New member` in create mode), keeping the `data-test-link="members-back"` on the Members link so the e2e contract survives. Removed the `border-b` under the header so the layout blends into the canvas the same way Ember's does.
  - Layout: sidebar **moved to the left** (`lg:flex-row-reverse` → `lg:flex-row`), matching Ember.
  - Sidebar now **live-follows the draft** in both edit and create: the header/avatar reflect what the user is typing before Save. In create mode the identity block shows "New member" as a placeholder until any field is filled — Ember has the equivalent Ember Data `isNew` fallback. The location / last-seen / signup-info blocks are hidden until the member is actually saved.
  - Form and Subscriptions are now inside Shade **`Card`** containers, matching the analytics screens' card style (bordered surface, `CardHeader`/`CardTitle`/`CardContent`).
  - Quality: detail unit suite 82/82, eslint clean, my files 0 tsc errors.
  - **Browser (e2e, real headless Chromium): 18/18 PASS** — all 18 existing tests, including the sidebar-visible check and the create-mode redirect, still pass under the new layout.
  - No new adversarial/Codex review for this slice — it's cohesive layout tweaks with zero behavioural change (draft/dirty/save flow untouched) and every existing test covers it.
- **slice 5.2 — cancel / continue subscription + Stripe links (Phase 5):** each paid subscription row now has a Shade `DropdownMenu` actions menu (`data-testid="subscription-actions"`) with two Stripe deep links (`dashboard.stripe.com/customers/<id>` and `.../subscriptions/<id>`) plus a state-aware Cancel / Continue subscription item that calls `useEditMemberSubscription` with `{cancel_at_period_end: true|false}` and toasts success/error. The mutation invalidates the members query so the row re-renders in its new state on refetch. Gift subs get **no menu** (Ember parity — gifts are managed via the gift flow). Comp subs also skip this menu; their comp-specific menu (Remove Complimentary) is Phase 6.
  - Trigger button is disabled and shows a spinner while the mutation is in flight so a user can't double-click into a duplicate request.
  - Quality: detail unit suite 82/82, eslint clean, my files 0 tsc errors.
  - **Browser (e2e, real headless Chromium): 18/18 PASS** — new tests: (i) cancel → status becomes "Canceled" and validity flips to "Has access until 15 Feb 2026"; menu now shows "Continue subscription"; continue → status returns to "Active" and validity flips back to "Renews 15 Feb 2026"; (ii) gift subscription row has no action menu at all.
  - **Codex + adversarial sub-agent reviews: both landed and folded in.** Real fixes:
    - **Stale menu race window** (both reviewers, highest-severity finding): after `useEditMemberSubscription` resolved, the mutation's `isLoading` flipped back before the invalidated members refetch landed, so a re-opened menu could fire the same action twice against the still-stale row. Fixed with the same `useIsFetching(['MembersResponseType'])` gate the suppression banner uses — the trigger stays disabled (and shows a spinner) until the refetch resolves.
    - **`EditMemberSubscriptionData` future footgun** (Codex): the hook payload previously allowed `{cancelAtPeriodEnd, status: 'canceled'}` together — tightened to a discriminated union so a caller can't accidentally send both at once (Phase 6 will use the `status: 'canceled'` variant for comping over a paid sub).
    - **e2e payload contract** (adversarial): the cancel/continue test now asserts the exact request bodies `[{cancel_at_period_end: true}, {cancel_at_period_end: false}]`, so a client-side field rename can't silently slip through while the UI still looks right.
  - Non-issues verified across both reviewers: menu accessibility (`aria-label` + `DropdownMenuTrigger asChild` valid Radix composition), Stripe URL parity with Ember (both hardcode prod dashboard), Stripe id contract (paid subs carry real Stripe ids so the URLs are always well-formed), `onSelect` (right choice — fires on keyboard + click), gift/comp gating (`kind === 'paid'` in the section covers it, pinned by the no-menu-for-gift e2e). Adversarial note: Ember doesn't toast on cancel/continue — my toast is a deliberate divergence matching the rest of this screen's mutation UX.
- **slice 5.1 — subscriptions display, correct paid/comped/gift model (Phase 5):** the reverted slice-2.3 attempt is redone against the real domain model. TDD'd pure helpers: `classifyMemberSubscription` (uses Ember's `!sub.id && plan.nickname.toLowerCase()` gate — the exact rule that distinguishes real Stripe paid subs from the zero-amount synthetics inside `member.subscriptions`), `formatSubscriptionAmount` (uses `toLocaleString` so `1000000` cents render `$10,000` not `$10000`, matching Ember `gh-price-amount` + `format-number` — the earlier attempt's regression), `formatSubscriptionInterval`, `getSubscriptionStatusLabel`, `getSubscriptionValidityLabel` (Renews / Has access until / Ended / Expires / Ends[trial], mirroring `validityDetails`), `groupSubscriptionsByTier`. `member-subscriptions-section.tsx` renders read-only rows grouped by tier, gated on `paid_members_enabled`; empty for a member with no subs (add-complimentary + comp remove are Phase 6). Framework types widened: `MemberSubscription.id: string | null` (comp/gift), `tier.expiry_at?`, `trial_end_at?`.
  - Quality: detail unit suite 80/80 (24 new subscription tests + 56 pre-existing), eslint clean, my files 0 tsc errors.
  - **Browser (e2e, real headless Chromium): 16/16 PASS** — three new tests inside a `test.use({stripeEnabled: true})` scope: paid row shows `$10.50 monthly` + `Renews on 15 Feb 2026`; complimentary row correctly reads `Complimentary` and NOT `$0 yearly` (pins the exact bug the earlier attempt had) and shows `Expires 15 Jan 2027`; a member with no subs renders no section. Subscriptions injected via the same narrow `page.route` regex used by the suppression test.
  - **Adversarial sub-agent review: folded in.** All real bugs it caught are fixed:
    - **Copy divergence**: `Renews on <date>` → `Renews <date>` (matches Ember `validityDetails` exactly — the e2e was pinning the wrong copy).
    - **Precedence**: hard-cancel and cancel-at-period-end now beat trial (matches Ember `hasEnded → willEndSoon → trialUntil`), so a canceled trial no longer misreports as "Ends".
    - **Comp/gift expiry timezone**: `tier.expiry_at` renders in UTC (Ember `.utc()`) rather than local — day-boundary drift for far-from-UTC admins is gone. `current_period_end` stays local, matching Ember.
    - **`sub.price` guard**: `groupSubscriptionsByTier` now filters `!!sub.price` before the row can crash on `sub.price.amount` (Ember does the same).
    - **`sub.tier` fallback**: also falls back to `sub.price.tier` (Ember hardening).
    - **`MemberSubscription.id` type**: narrowed from `string | null` back to `string` and comp/gift now use `id: ''` in tests + e2e — verified against `member-bread-service.js:127`, which synthesises comps/gifts with empty-string ids.
    - **Locale-fragile test**: swapped hard-coded `$10,000` for a regex accepting `.` or `,` as separator, so the test passes on any dev/CI locale (runtime still respects the user's browser locale).
  - **i18n note (adversarial finding #4, deferred):** no strings are wrapped in `t()` on this screen — matches the rest of Phase 2–5 output. Whole surface will be i18n'd in a dedicated cutover pass, tracked separately.
  - **Discounts / offers (deferrable, adversarial #11):** the `hasActiveDiscount` / retention-offer display from Ember is not rendered here; folded into Phase 6.
  - **Codex review:** launched at high effort; still running in the background at commit time — matches the pattern of previous phases. Any new findings get a follow-up commit.
- **slice 4.2 — email suppression banner + re-enable (Phase 4):** TDD'd `getMemberSuppressionInfo` (5 unit tests for suppressed/non-suppressed states + `fail`/`spam`/other reason labels mirroring Ember copy). The newsletters field now branches on suppression: when the member is suppressed the toggles are replaced by a banner (`data-testid="member-suppression-banner"`) with the reason/date label + a "Re-enable email" button that calls `useRemoveMemberEmailSuppression` and shows a success/error toast; mutation invalidates the members query so the banner clears on refetch.
  - Quality: detail unit suite 56/56, eslint clean, my files 0 tsc errors.
  - **Browser (e2e, real headless Chromium): 13/13 PASS** — new "shows a suppression banner instead of newsletter toggles for a bounced member" test uses Playwright's `page.route` to inject `email_suppression` into the members read (the suppression list is populated by external delivery events; intercepting the read is cheaper than reaching into the mailgun pipeline).
  - **Adversarial sub-agent + Codex reviews: both landed and folded in.** Real fixes:
    - **`suppressed:true` without an `info` block now renders the banner** (label omitted) rather than falling through to newsletter toggles — Ember does the same for a member whose `email_disabled` was flipped after the Mailgun row was cleaned; my earlier behaviour would have let admins toggle newsletters on an email-disabled member.
    - **Re-enable button stays busy until the invalidated members refetch lands** (via `useIsFetching(['MembersResponseType'])`) and shows a spinner + `sr-only` label — kills the spam-clickable window Codex + adversarial both flagged where the mutation resolved before the cache did.
    - **Timezone parity with Ember**: `moment.utc(...)` swapped for `moment(new Date(...))` so admins far from UTC don't see off-by-one dates on near-midnight suppressions.
    - **e2e route pattern narrowed** to the specific `.../members/<id>/` GET (regex) with a `body.members?.[0]` guard so future intercepts can't accidentally throw on unrelated response shapes (subscriptions/signin_urls/etc.).
    - Added a **"Learn more"** link to the banner + aligned toast copy to Ember ("Email re-enabled successfully" / "Failed to re-enable email. Please try again.").
    - **New e2e** exercises the full re-enable path: DELETE stub returns 204, the mocked members read flips to `suppressed:false` so the invalidation-driven refetch clears the banner and shows the success toast — closes the coverage gap Codex flagged (test was previously banner-render-only, no mutation path).
  - Non-issues verified: server never sends `'bounce'` as a reason (Mailgun normalises to `'fail'` at `mailgun-email-suppression-list.js:79`); the newsletters query firing while suppressed pre-warms the toggle set for after re-enable — acceptable one small request.
- **slice 4.1 — newsletter toggles (Phase 4):** extended the editable draft with `newsletters: string[]` (sorted ids); TDD'd `toggleMemberNewsletter`, `getMemberNewslettersUiEnabled`, and a critical safety helper `buildMemberFieldEditPayload` that **omits `newsletters` from the payload when unchanged**. Reason: verified the server replaces the subscription set with exactly what's sent (`memberData.newsletters ||` gate in `member-repository.js:640`), so a bare `newsletters: []` on a name-only save would unsubscribe the member from every active newsletter — a real data-loss bug I caught pre-implementation and pinned with a unit test. Also refactored dirty-comparison into `normalizeDraftForComparison` to keep string-trimming while leaving already-normalized arrays alone. Newsletters field renders a Shade `Switch` per active newsletter, gated by `editor_default_email_recipients !== 'disabled'`.
  - Quality: detail unit suite 51/51, eslint clean, my files 0 tsc errors.
  - **Browser (e2e, real headless Chromium): 12/12 PASS** — including the "toggle newsletter + save + reload → subscription persists" round-trip (proves the payload actually reaches the server and syncs correctly).
  - **Adversarial sub-agent review: folded in.** Real fixes: (i) newsletters section is now hidden in **create mode** — the create payload doesn't send `newsletters`, and the server auto-subscribes via `subscribe_on_signup`, so showing toggles would have silently discarded the user's choices (pinned by an e2e assertion); (ii) the newsletters field now hides its own **heading + body together** when loading or when a site has zero active newsletters, so users never see a bare label with an empty body; (iii) reworded the misleading "flash in vs flash out" comment on `getMemberNewslettersUiEnabled` to describe what the code actually does (bias for the common case). Reviewed and confirmed safe (no code change needed): archived-newsletter data loss (server re-pushes them on `member-repository.js:791-798`); loading race (draft carries correct ids even before the toggle UI mounts, and the omit-check prevents any accidental unsubscribe). Noted for later: the 50-newsletter first-page cap in `useBrowseNewsletters` (real ceiling, harmless up to that scale; slice 4.2's suppression banner is the remaining Phase-4 parity gap — that's exactly the next slice).
  - **Codex review:** queued to the background runner; treating as best-effort. Committing on the strength of the adversarial review + full coverage; a follow-up commit is on standby if Codex surfaces anything new.
- **slice 3.3 — new-member create mode (Phase 3):** the detail component now branches on `isCreating = memberId === 'new'` (create screen reuses the preview route via the sentinel id `new` → `/members/preview/new`): skips the fetch, seeds an empty draft, measures dirty against an empty baseline, requires a valid email, saves via `useAddMember`, and on success redirects to `/members/preview/<newId>` (edit mode). A `bypassGuardRef` lets that redirect through the unsaved-changes blocker; it resets on route change. Widened the framework `NewMember` type with optional `note` + `labels`.
  - Quality: detail unit suite 40/40, eslint clean, my files 0 tsc errors (regenerated the framework declaration types so the widened `NewMember` resolves — the dev `vite build --watch` doesn't re-emit `.d.ts`).
  - **Browser (e2e, real headless Chromium): 11/11 PASS** — new "creates a new member and redirects to their detail" (New member title, Save gated on email, create → redirect to the member's edit screen with data persisted).
  - **Adversarial sub-agent review: folded in.** Traced the create→edit guard race and confirmed **safe** (`bypassGuardRef` set synchronously before `navigate`; reset on `memberId` change) — no false-block, no lingering bypass. Real fixes applied: create-with-no-member-id response now shows a toast error instead of silently no-op'ing (prevents accidental duplicate on resubmit); added a one-line comment noting the `new` sentinel can't collide with 24-char hex ObjectIds; the e2e now asserts the created member comes back `subscribed:true` with newsletters attached, pinning Ember parity so a future server-default change can't silently regress it. Noted for the Phase-8 cutover checklist: add a static `/members/new` route ranking above `:member_id`, remove `/members/new` from `EMBER_ROUTES`, and rewire `members-empty-state.tsx` / `members-actions.tsx` "New member" links from the hash-nav to React nav.
  - **Codex review:** launched at high effort; ran ~16 minutes and timed out mid-synthesis (same behavioural pattern as slice 3.2). It had cross-referenced the right files (`member-detail.tsx`, `member-detail-edit.ts`, `api/members.ts`, `hooks.ts`, `api/endpoints/members.js`, `models/member.js`, `routes.tsx`, `ghost/admin/app/routes/member.js`, the e2e test) and was mid-trace on the labels `undefined`-vs-`[]` question and `bypassGuardRef`/`useBlocker` redirect safety — both of which the adversarial review already verified — but never produced ranked findings. Committing on the strength of the adversarial review + full coverage; a follow-up commit is on standby if a later Codex re-run surfaces anything new.
- **slice 3.2 — labels field (Phase 3):** extended the editable draft/slice to carry `labels: {name,slug}[]` (sorted by slug) with dirty detection + save payload (TDD: label normalization, `isDraftInSyncWithServer` label diff, `resolveSlugsToLabels`); `member-labels-field.tsx` reuses the existing `LabelPicker`/`useLabelPicker`. Verified server truth: member model matches labels **by name** and `unset`s when omitted, so the payload sends `[{name,slug}]` and `[]` removes all. Create-timing handled: `handleCreate` records a freshly-created label's real name before re-resolving so it's never saved as its slug (which would dup-create).
  - Quality: detail unit suite 40/40, eslint clean, my files 0 tsc errors.
  - **Browser (e2e, real headless Chromium): 10/10 PASS** — including remove-existing-label and add-new-label-via-picker, each persisting across reload (proves labels come back from the read, the factory persists them, and the create path saves the correct name).
  - **Adversarial sub-agent review: folded in.** Root cause it found: label name resolution happened *after* an await via a stale render closure + a monotonic cache. Fixes applied: `handleCreate` now reconciles against a live `labelsRef` and guarantees the created label is present with its real name (no stale-closure resurrection, no transient slug-as-name that could dup-create for names a slug can't reproduce); the disabled state uses `inert` (truly non-interactive during save, not just `pointer-events-none`); label sort uses deterministic byte comparison instead of `localeCompare`. Re-verified: unit 40/40, e2e 10/10.
    - Noted for the cutover slice (out of this slice's scope — the clean fix is centralizing slug→name resolution inside the shared `useLabelPicker`): a label renamed out-of-band and not in the current picker page keeps a stale cached name; and the `<Label>Labels</Label>` isn't programmatically associated with the combobox (a11y). Also flagged (pre-existing, inherited from `LabelPicker`, not introduced here): raw palette utilities in `label-picker.tsx` vs Shade tokens.
  - **Codex review:** launched at high effort but the run exceeded ~18 min without returning; a background monitor will deliver its findings when it completes, and any new issues get a follow-up commit. **Committing now** on the strength of the adversarial review + full test/e2e coverage.
- **slice 3.1 — editable name/email/note + save + dirty guard (Phase 3):** TDD `getMemberEditableSlice` (trim/null-normalize) + `isValidMemberEmail` (6 unit tests, red→green); `member-detail-form.tsx` (Name/Email/Note inputs with role=textbox names + 500-char note counter); `member-detail.tsx` now owns a `draft` (seeded per member via `seededIdRef`), `dequal` dirty detection, `useEditMember` save, a Save/Saving/Saved/Retry button, and `useConfirmUnload` + `useBlocker` + a "Leave" discard dialog — mirroring the Automations editor pattern.
  - Quality: detail unit suite 29/29, eslint clean, my files 0 tsc errors (env noise aside).
  - **Browser (e2e, real headless Chromium): 8/8 PASS** — edits a name and it persists across reload; warns + "Leave" before navigating away with unsaved changes; invalid email disables Save.
  - **Reviews: Codex + adversarial sub-agent, both folded in.** Verified independently that the `{id,name,email,note}` save payload does NOT wipe labels/newsletters/tiers (server does partial updates — `member-repository.js:637-640`). Fixes applied from the reviews:
    - note field: removed the hard `maxLength=500` (Ember's 500 is a *soft* countdown; DB allows 2000) and switched the counter to unicode **code-point** counting (emoji-safe) via a TDD'd `getNoteCharactersLeft`; goes negative/red like Ember.
    - Save button: "Retry" now only shows while there are unsaved changes and is gated on email validity (no enabled-but-no-op Retry); prior save error is `reset()` on the next edit.
    - empty email now shows "Email is required." instead of silently disabling Save.
    - **in-flight edit clobber** (Codex P1): fields are disabled while saving, so a save response can't overwrite edits typed mid-request.
    - **same-id refetch false-dirty** (Codex P1): draft now reseeds from fresh server data when the user hasn't diverged (TDD'd `isDraftInSyncWithServer`), so a background refetch never fabricates "unsaved changes".
    - **whitespace-only false-dirty** (Codex P3): dirty check compares the *normalized* draft against the server slice.
    - **cross-member guard**: `draftMemberIdRef` seeds fresh per member and `onSave` refuses a draft belonging to another member.
    - Noted for the cutover slice (low reachability): one-frame post-save "Saved→Save" flicker (self-heals on refetch); react-query mutation state (`isSuccess`/`isError`) could leak across members if in-app member→member navigation is added (there is none yet).
  - Final: unit **36/36**, e2e **8/8** (real browser), lint + tsc clean. **Committed.**
- **slice 2.3 — read-only subscriptions: ATTEMPTED, then REVERTED (not committed).** Built TDD'd helpers + a `member-subscriptions-section.tsx`, all passing (30 unit, e2e 6/6). The **adversarial review caught a HIGH-severity model bug before commit** (the review process working as intended): complimentary & gift subscriptions are **real zero-amount Stripe subscriptions inside `member.subscriptions`, classified by `price.nickname`** ('Complimentary' / 'Gift Subscription'), *not* entries in `member.tiers`. My component keyed comps off `member.tiers` (a dead branch for real comped members) and would have rendered comp/gift members as bogus "$0 yearly / Renews on…" paid rows and dropped gift members. Secondary findings: amounts ≥ $1,000 lose thousands separators vs Ember `gh-price-amount` (drops the locale step); `getSubscriptionInterval` labels any non-year interval "monthly"; `sub.price` deref is unguarded though Ember filters `!!sub.price`.
  - **Decision (deviation D4):** faithful subscription rendering (comp/gift classification via nickname, validity/price labels, per-tier grouping, discounts/offers) is genuinely **Phase 5 (subscriptions)** work and needs Stripe-backed e2e to verify real rows — so it's deferred there rather than shipped half-modelled in the Phase 2 read-only scaffold. Reverted the component + helpers; `member-detail.tsx` + e2e restored to the slice-2.2 state (23 detail unit tests still green).
  - **Newsletters** likewise belong in Phase 4 (interactive: toggles + suppression re-enable), not a throwaway read-only version here.
  - Net: **Phase 2 read-only scaffold = slices 2.1 (route/tracer) + 2.2 (sidebar)**, committed and verified. The remaining member-body sections are built in their dedicated phases (fields/create → P3, newsletters → P4, subscriptions → P5, comp tiers → P6, actions → P7, activity → P8). Key domain fact for P5 captured in memory (`project_member_detail_react_migration`).
- **slice 2.2 — read-only sidebar:** TDD 3 pure helpers (`parseMemberGeolocation`, `formatMemberLocation`, `getMemberReferrerSource`) mirroring Ember `gh-member-details` (12 unit tests, red→green); `member-detail-sidebar.tsx` (avatar, location, last-seen, comments-disabled indicator, signup info) wired into a two-column body; added `attribution` to the framework `Member` type.
  - Scoping: **engagement stats moved to slice 2.3** (they share the `editorDefaultEmailRecipients` settings gate with newsletters).
  - Quality: unit 20/20 (detail dir), eslint clean. Types: `attribution` resolves after regenerating the framework declaration types; my files otherwise clean. (Note: local bare `tsc` in apps/posts emits ~165 "cannot be used as a JSX component" errors app-wide from duplicated `@types/react` 17/18 in node_modules — pre-existing env noise, not from this work; CI installs clean. The framework `dev` watch is `vite build --watch` which does NOT re-emit `.d.ts`, so consuming apps see stale framework types until a declaration build runs.)
  - **Browser (e2e, real headless Chromium): 5/5 PASS** — sidebar visible, location renders ("Unknown location" for API-created members), "Created —" date shown.
  - **Adversarial review (sub-agent): "ship it"** — no correctness defects vs Ember (traced every location branch); XSS on the attribution link is not a real risk (server-derived URL, at parity with Ember). Actioned its low-priority notes: made `parseMemberGeolocation` reject arrays honestly (`!Array.isArray`); added the missing regression cases (US-no-region-with-country, array input, empty object, empty-string source) → format tests now 15; corrected the DRY comment (list helper differs in control flow, not just the fallback string).
  - **Codex second opinion:** unavailable (see Tooling notes).
  - **Committed.**
- **slice 2.1:** TDD `deriveMemberDetailBackPath` (6 unit tests, red→green); minimal read-only `member-detail.tsx` (title + back link + not-found); `./member-detail` export; temp preview route `/members/preview/:member_id`.
  - Quality: unit 6/6, eslint clean, new files 0 tsc errors.
  - **Browser (e2e, dev mode, real headless Chromium): 4/4 PASS** — member name renders on the React screen; back link returns to `/members`. Confirms the new package export resolves live + temp route + `data-test-link="members-back"` contract.
  - **Adversarial review (sub-agent):** validated the slice. Confirmed the `deriveMemberDetailBackPath` open-redirect angle is **safe** (prefix guard mirrors Ember; rejects the only exploitable vectors `//host` + `https://host`; internal false-positives stay same-origin under the hash router). Actioned findings: fixed a real cosmetic bug (`truncate` on the `<h1>` needed `min-w-0` to work in the flex header); added `enabled: !!memberId` guard on `getMember`; added 2 characterization tests (protocol-relative rejection + intentional Ember-parity prefix behaviour) → nav tests now 8/8. Non-blocking notes logged for cutover: hardcoded English strings (i18n TODO), 404-vs-transient-error conflation.
  - **Codex second opinion:** unavailable (corrupted install, see Tooling notes).
  - **Committed.**
