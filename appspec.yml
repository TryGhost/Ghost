version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/blog
hooks:
  AfterInstall:
    - location: /var/www/blog/bin/install.sh #file is located in the EC2
      timeout: 900
      runas: root
