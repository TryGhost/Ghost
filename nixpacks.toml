cmds = [
  """export NVM_DIR="$HOME/.nvm" &&
     [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh" &&
     nvm install 22.13.1 &&
     nvm use 22.13.1 &&
     corepack enable &&
     yarn install --frozen-lockfile"""
]
