{
    "name": "Ghost Local DevContainer",
    "build": {
        "context": "..",
        "dockerfile": "./.docker/Dockerfile",
        "target": "base-devcontainer"
    },
    "workspaceFolder": "/home/ghost",
    "workspaceMount": "source=${localWorkspaceFolder},target=/home/ghost,type=bind"
}
