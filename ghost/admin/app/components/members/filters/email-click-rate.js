import {NUMBER_RELATION_OPTIONS} from './relation-options';

export const EMAIL_CLICK_RATE_FILTER = {
    label: 'Click rate (all time)',
    name: 'email_click_rate',
    valueType: 'number',
    setting: 'emailTrackClicks',
    relationOptions: NUMBER_RELATION_OPTIONS
};
