<GhPaginatedTokenInput
    @allowCreation={{false}}
    @disabled={{@disabled}}
    @loadPage={{this.loadAuthorsPage}}
    @loadingMessage="Loading authors..."
    @matcher={{this.matchAuthor}}
    @onChange={{@onChange}}
    @placeholder={{@placeholder}}
    @renderInPlace={{@renderInPlace}}
    @searchDebounceMs={{this.searchDebounceMs}}
    @searchField={{null}}
    @searchPage={{this.searchAuthorsPage}}
    @selected={{@selected}}
    @selectedItemComponent={{@selectedItemComponent}}
    @sortOptions={{this.sortAuthors}}
    @triggerClass={{@triggerClass}}
    @triggerId={{@triggerId}}
/>
