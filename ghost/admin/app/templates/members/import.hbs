{{#unless this.feature.membersForward}}
    <GhFullscreenModal @modal="import-members"
        @confirm={{this.refreshMembers}}
        @close={{this.close}}
        @modifier="action import-members" />
{{/unless}}
