SCHEMA >
    `site_uuid` UUID,
    `id` String,
    `automation_id` String,
    `created_at` DateTime,
    `updated_at` DateTime

ENGINE ReplacingMergeTree
ENGINE_SORTING_KEY site_uuid, id
ENGINE_VER updated_at
