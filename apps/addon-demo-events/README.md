# Simple Events add-on demo

An independently installable editor-block add-on that demonstrates equivalent static web, email, and RSS output without a public hydration runtime.

Run `pnpm dev`, then install `http://localhost:4651/manifest.json` through the add-on spike.
