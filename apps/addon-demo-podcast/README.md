# Podcast Player add-on demo

An independently installable editor-block add-on that resolves public episode pages through its own backend, saves a durable static presentation, and hydrates a public audio player.

Run `pnpm dev`, then install `http://localhost:4652/manifest.json` through the add-on spike.
