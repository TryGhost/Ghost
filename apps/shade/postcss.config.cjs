/**
 * In TailwindCSS 4 no postcss plugins are needed. Additionally, since we're
 * running vite, we can use the @tailwindcss/vite plugin for better performance.
 */

module.exports = {
    plugins: {}
};
